services:
  - type: web
    name: nexora-backend
    env: node
    buildCommand: npm install && cd backend && npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 5000
      - key: NODE_ENV
        value: production
